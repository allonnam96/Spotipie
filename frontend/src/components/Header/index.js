import "./Header.css"

const Header = () => {


// return (
    // make the image and wrap the div
    // have on click to the div and redirect to the homepage
// )
const handleClick = e => {
    //redirect
}

return (
    <div id = "header">
        <div id = "header-logo" onClick={handleClick}>
        {/* import a pic */}
            <img src=""/> 
            Spotipie
        </div>
    </div>
)
}

export default Header